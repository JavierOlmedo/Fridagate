package com.hackpuntes.fridagate.ui.theme

import androidx.compose.ui.graphics.Color

// ─────────────────────────────────────────────────────────────────────────────
// Fridagate color palette — hacker / dark terminal aesthetic
// Based on the rePloit design: dark navy background + electric cyan accent
// ─────────────────────────────────────────────────────────────────────────────

// Primary — electric cyan (buttons, highlights, active states)
val CyanPrimary       = Color(0xFF00D4FF)
val CyanPrimaryDark   = Color(0xFF00A8CC)

// Secondary — blue (secondary actions, links)
val BluePrimary       = Color(0xFF0070FF)
val BluePrimaryDark   = Color(0xFF0055CC)

// Backgrounds — dark navy layers
val BackgroundDark    = Color(0xFF080D18) // Deepest background
val SurfaceDark       = Color(0xFF0F1623) // Cards and containers
val SurfaceVariant    = Color(0xFF141C2E) // Slightly lighter surface

// Text colors
val TextPrimary       = Color(0xFFFFFFFF) // Main text — pure white
val TextSecondary     = Color(0xFF8A9BC0) // Secondary / muted text
val TextDisabled      = Color(0xFF3D4F6E) // Disabled text

// Status colors
val GreenSuccess      = Color(0xFF00FF88) // "System Online" green
val RedError          = Color(0xFFFF3B5C) // Error / danger
val OrangeWarning     = Color(0xFFFFAA00) // Warning

// Border / divider
val BorderColor       = Color(0xFF1E2D45) // Subtle border on cards
